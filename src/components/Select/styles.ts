import { Select, styled } from '@mui/material'

const SelectWrapper = styled(Select)`
    .MuiOutlinedInput-notchedOutline {
        border: none;
    }
    .MuiSelect-select {
        padding: 1.0313rem 0.25rem;
    }
`

export { SelectWrapper }
